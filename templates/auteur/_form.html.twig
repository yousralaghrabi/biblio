{{ form_start(form, {'attr': {'class': 'needs-validation', 'novalidate': ''}}) }}
    <div class="row">
        <div class="col-md-6 mb-3">
            {{ form_label(form.nom, null, {'label_attr': {'class': 'form-label'}}) }}
            {{ form_widget(form.nom, {'attr': {
                'class': 'form-control',
                'required': (form.nom.vars.required ? 'required' : null),
                'aria-required': (form.nom.vars.required ? 'true' : 'false'),
                'aria-describedby': (form.nom.vars.errors|length ? form.nom.vars.id ~ '_errors' : null)
            }}) }}
            <div id="{{ form.nom.vars.id }}_errors" class="form-text text-danger" aria-live="polite">{{ form_errors(form.nom) }}</div>
        </div>
        <div class="col-md-6 mb-3">
            {{ form_label(form.prenom, null, {'label_attr': {'class': 'form-label'}}) }}
            {{ form_widget(form.prenom, {'attr': {
                'class': 'form-control',
                'required': (form.prenom.vars.required ? 'required' : null),
                'aria-required': (form.prenom.vars.required ? 'true' : 'false'),
                'aria-describedby': (form.prenom.vars.errors|length ? form.prenom.vars.id ~ '_errors' : null)
            }}) }}
            <div id="{{ form.prenom.vars.id }}_errors" class="form-text text-danger" aria-live="polite">{{ form_errors(form.prenom) }}</div>
        </div>
        <div class="col-md-6 mb-3">
            {{ form_label(form.datenaissance, null, {'label_attr': {'class': 'form-label'}}) }}
            {{ form_widget(form.datenaissance, {'attr': {
                'class': 'form-control',
                'required': (form.datenaissance.vars.required ? 'required' : null),
                'aria-required': (form.datenaissance.vars.required ? 'true' : 'false'),
                'aria-describedby': (form.datenaissance.vars.errors|length ? form.datenaissance.vars.id ~ '_errors' : null)
            }}) }}
            <div id="{{ form.datenaissance.vars.id }}_errors" class="form-text text-danger" aria-live="polite">{{ form_errors(form.datenaissance) }}</div>
        </div>
        <div class="col-md-6 mb-3">
            {{ form_label(form.nationalite, null, {'label_attr': {'class': 'form-label'}}) }}
            {{ form_widget(form.nationalite, {'attr': {
                'class': 'form-control',
                'required': (form.nationalite.vars.required ? 'required' : null),
                'aria-required': (form.nationalite.vars.required ? 'true' : 'false'),
                'aria-describedby': (form.nationalite.vars.errors|length ? form.nationalite.vars.id ~ '_errors' : null)
            }}) }}
            <div id="{{ form.nationalite.vars.id }}_errors" class="form-text text-danger" aria-live="polite">{{ form_errors(form.nationalite) }}</div>
        </div>
        <div class="col-12 mb-3">
            {{ form_label(form.biographie, null, {'label_attr': {'class': 'form-label'}}) }}
            {{ form_widget(form.biographie, {'attr': {
                'class': 'form-control',
                'rows': 5,
                'required': (form.biographie.vars.required ? 'required' : null),
                'aria-required': (form.biographie.vars.required ? 'true' : 'false'),
                'aria-describedby': (form.biographie.vars.errors|length ? form.biographie.vars.id ~ '_errors' : null)
            }}) }}
            <div id="{{ form.biographie.vars.id }}_errors" class="form-text text-danger" aria-live="polite">{{ form_errors(form.biographie) }}</div>
        </div>
    </div>
    <div class="action-buttons mt-4">
        <button type="submit" class="btn btn-info btn-icon">
            <i class="bi bi-check-circle"></i> {{ button_label|default('Save') }}
        </button>
        <a href="{{ path('app_auteur_index') }}" class="btn btn-outline-secondary btn-icon">
            <i class="bi bi-x-circle"></i> Cancel
        </a>
    </div>
{{ form_end(form) }}
