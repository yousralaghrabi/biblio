{% extends 'base.html.twig' %}

{% block title %}Edit Author{% endblock %}

{% block body %}
<div class="container mt-4">
    <div class="page-header">
        <h1><i class="bi bi-pencil"></i> Edit Author</h1>
        <p class="subtitle">Update the author information</p>
    </div>

    <div class="card">
        <div class="card-body">
            {{ include('auteur/_form.html.twig', {'button_label': 'Update Author'}) }}
        </div>
        <div class="card-footer bg-light">
            <div class="action-buttons">
                {{ include('auteur/_delete_form.html.twig') }}
            </div>
        </div>
    </div>
</div>
{% endblock %}
