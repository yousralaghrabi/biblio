{% extends 'base.html.twig' %}

{% block title %}New Category{% endblock %}

{% block body %}
<div class="container mt-4">
    <div class="page-header">
        <h1><i class="bi bi-plus-circle"></i> Add New Category</h1>
        <p class="subtitle">Fill in the details to add a new category</p>
    </div>

    <div class="card">
        <div class="card-body">
            {{ include('categorie/_form.html.twig', {'button_label': 'Create Category'}) }}
        </div>
    </div>
</div>
{% endblock %}
