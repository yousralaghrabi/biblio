{% extends 'base.html.twig' %}

{% block title %}Edit Member{% endblock %}

{% block body %}
<div class="container mt-4">
    <div class="page-header">
        <h1><i class="bi bi-pencil"></i> Edit Member</h1>
        <p class="subtitle">Update the member information</p>
    </div>

    <div class="card">
        <div class="card-body">
            {{ include('adherent/_form.html.twig', {'button_label': 'Update Member'}) }}
        </div>
        <div class="card-footer bg-light">
            <div class="action-buttons">
                {{ include('adherent/_delete_form.html.twig') }}
            </div>
        </div>
    </div>
</div>
{% endblock %}
