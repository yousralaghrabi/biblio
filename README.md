# mini_app

Symfony-based mini app. To run locally:

1. Install dependencies: `composer install`
2. Copy `.env` from `.env.example` and configure DB
3. Run migrations and start the server

Before publishing, ensure `.env` contains no secrets and is listed in `.gitignore`.
